// Single text field — user describes need in natural language
// Triggers Wispr Agent → live animated blueprint construction

export default function ChatPage() {
  return <main>Intent input — TODO</main>;
}
