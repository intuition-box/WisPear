// Curator dashboard
//   - Active stakes + domain expertise scores
//   - Pending peer validations (agree-with / dispute)
//   - Feedback API signals on staked components

export default function DashboardPage() {
  return <main>Curator dashboard — TODO</main>;
}
