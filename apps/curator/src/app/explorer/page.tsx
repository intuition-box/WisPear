// Explorer — two tabs
//   Components: catalogue filtered by profile, live trust scores
//   Community Sets: browse blueprints, filter by role/level,
//                   stake $TRUST to unlock (bonding curve)

export default function ExplorerPage() {
  return <main>Explorer — TODO</main>;
}
